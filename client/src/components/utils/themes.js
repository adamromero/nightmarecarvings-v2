import { blue, orange, red, green, purple } from "./colors";
import { primaryFont } from "./typography";

export const addTheme = {
   color: orange[100],
   hover: orange[200],
};

export const deleteTheme = {
   color: red[100],
   hover: red[200],
};

export const editTheme = {
   color: purple[100],
   hover: purple[200],
};

export const clearTheme = {
   color: blue[100],
   hover: blue[200],
};

export const completedTheme = green;
